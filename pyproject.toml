[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "spectral-io"
requires-python = ">=3.9"
dependencies = ["numpy"]

[tool.maturin]
features = ["python"]
module-name = "spectral_io"
